{
  "name": "imdb",
  "version": "1.0.0",
  "description": "title = $('div[class=\"title_wrapper\"] > h1').text().trim() status = $('div[class=\"ratingValue\"] > strong > span').text() summary = $('div[class=\"ipc-html-content ipc-html-content--base\"] > div').text().trim() release date = $('a[title=\"See more release dates\"]').text().trim()",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cheerio": "^1.0.0-rc.5",
    "json2csv": "^5.0.5",
    "request": "^2.88.2",
    "request-promise": "^4.2.6"
  }
}
